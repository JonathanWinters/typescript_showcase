export enum MyEnum {
    A = 0,
    B = 1
}

export enum Colors {
    Red,
    Green
}

/* we really expect these to be like: */
const MY_ENUM = {
    A: 0,
    B: 1
}

const COLORS = {
    Red: 0,
    Green: 1
}
